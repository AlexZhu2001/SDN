# This Project is using for learning SDN

* basic -> basic network topo and mininet practice
* bridge -> using mininet create bridge and vlan
* containernet -> using containernet create hosts by docker